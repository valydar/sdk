# SonarQube Coverage Gap — sdk#21

## Summary

The SonarQube quality gate requires `new_coverage >= 80%` but Flutter/Dart and iOS/Swift code cannot be analyzed because the corresponding language plugins are missing from SonarQube Community Build 26.9.0.129388.

## Findings

### ARC Runner Tools Availability (`summerwind/actions-runner-dind:v2.337.0-ubuntu-24.04`)

| Tool | Available | Notes |
|------|-----------|-------|
| `dart` | ❌ | Not installed on runner image |
| `flutter` | ❌ | Not installed on runner image |
| `dotnet` | ❌ | Not installed on runner image |
| `swift` | ❌ | Not installed on runner image |
| `swiftlint` | ❌ | Not installed on runner image |

### SonarQube Language Analyzers (Community Build 26.9.0.129388)

| Language | Plugin | Status |
|----------|--------|--------|
| C# | `sonar-csharp-plugin-10.33.0.1635.jar` | ✅ Installed |
| Java | `sonar-java-plugin-8.41.0.47177.jar` | ✅ Installed |
| JavaScript/TypeScript | `sonar-javascript-plugin-13.8.0.44569-multi.jar` | ✅ Installed |
| Python | `sonar-python-plugin-5.31.0.36502.jar` | ✅ Installed |
| Kotlin | `sonar-kotlin-plugin-3.9.0.9809.jar` | ✅ Installed |
| **Dart (Flutter)** | — | ❌ **NOT installed** |
| **Swift (iOS)** | — | ❌ **NOT installed** |

### Current Quality Gate Status

- `new_coverage`: **0.0%** (threshold: 80%) → **ERROR**
- `new_duplicated_lines_density`: 0.0% (threshold: 3%) → OK
- `new_violations`: 0 (threshold: >0) → OK

### SonarQube Project Components

Registered components: `android`, `csharp/Valydar`, `java`, `python`, `react-native`

**Missing components**: `flutter/`, `ios/` — not registered because SonarQube has no Dart/Swift analyzers to detect them.

## Recommended Actions

1. **Install Dart plugin** in SonarQube Community Build (SonarDart or compatible) to enable Flutter analysis
2. **Install Swift plugin** in SonarQube Community Build (SonarSwift or compatible) to enable iOS analysis
3. **Switch the Sonar workflow to SonarScanner for .NET** and run a real `dotnet build` for `csharp/Valydar`; installing the .NET SDK alone is not sufficient with the current CLI-only scanner setup
4. **Install .NET SDK** on the ARC runner image so the workflow can run the required `dotnet build -warnaserror` step for C# analysis
5. **Install Dart SDK** on the ARC runner image to enable `dart analyze --fatal-infos`

## Per PM Pre-Approved Plan (sdk#21)

- `dart analyze --fatal-infos` first → **dart not available on runner** → documented
- `dotnet build -warnaserror` → **dotnet not available on runner** → documented
- `swiftlint` → **swiftlint not available on runner** → documented

## Coverage Gap Impact

Since the leak baseline was set on 2026-09-12, all new code in `flutter/` and `ios/` directories counts as uncovered (0%), causing the quality gate to fail with `new_coverage < 80%`.

---

*Generated by Lancelot for sdk#21 — Closes #21*
